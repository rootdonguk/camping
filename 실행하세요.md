# 🎯 캠핑장 예약 시스템 - 즉시 실행 가이드

## ⚡ 가장 빠른 방법 (3초 안에 시작)

### Windows
```cmd
auto-install.bat
```
파일을 더블클릭하거나 명령 프롬프트에서 실행

### Mac/Linux
```bash
./auto-install.sh
```
터미널에서 실행

### 모든 OS (Python 사용)
```bash
python quick-start.py
```

---

## 🎁 완전 자동화된 기능

✅ **Node.js 자동 설치**
- Windows: Chocolatey 또는 브라우저 다운로드
- Mac: Homebrew로 자동 설치
- Linux: apt/dnf/yum으로 자동 설치

✅ **pnpm 자동 설치**
- npm 또는 공식 스크립트로 설치

✅ **MySQL 설치 안내**
- 설치 여부 확인
- 미설치 시 자동 설치 또는 다운로드 페이지 안내

✅ **.env 파일 자동 생성**
- DATABASE_URL 입력 받기
- STRIPE_SECRET_KEY 설정 (선택)

✅ **의존성 자동 설치**
- pnpm install 자동 실행

✅ **데이터베이스 마이그레이션**
- 테이블 자동 생성

✅ **서버 자동 시작**
- http://localhost:3000 에서 즉시 실행

---

## 📝 실행 중 입력할 정보

### 1. DATABASE_URL (필수)
```
mysql://사용자명:비밀번호@호스트:포트/데이터베이스명
```

**예시:**
```
mysql://root:mypassword@localhost:3306/camping_db
```

### 2. STRIPE_SECRET_KEY (선택사항)
```
sk_test_51abc123def456...
```
Enter를 눌러 건너뛸 수 있습니다.

---

## 🎮 실행 후 할 일

### 1. 브라우저 열기
```
http://localhost:3000
```

### 2. 관리자 계정 설정
첫 로그인 시 자동으로 관리자 권한이 부여됩니다.

### 3. 주요 기능 설정

#### 은행 계좌 추가 (관리자)
```typescript
POST /api/trpc/bankAccounts.create
{
  "bankName": "국민은행",
  "accountNumber": "123-456-789012",
  "accountHolder": "캠핑장"
}
```

#### 결제 게이트웨이 설정 (관리자)
```typescript
POST /api/trpc/paymentGateways.upsert
{
  "provider": "naver_pay",
  "isEnabled": true,
  "merchantId": "...",
  "apiKey": "..."
}
```

#### 캠핑장 사이트 추가 (관리자)
```typescript
POST /api/trpc/sites.create
{
  "name": "A구역",
  "capacity": 4,
  "pricePerNight": "50000",
  "siteType": "tent"
}
```

---

## 🔧 문제 해결

### Node.js 설치 실패
- Windows: https://nodejs.org 에서 수동 다운로드
- Mac: `brew install node`
- Linux: 배포판별 패키지 매니저 사용

### MySQL 연결 오류
1. MySQL 서비스가 실행 중인지 확인
2. DATABASE_URL의 사용자명/비밀번호 확인
3. 데이터베이스가 생성되어 있는지 확인

### 포트 충돌 (3000번 포트 사용 중)
`.env` 파일에 추가:
```env
PORT=3001
```

### pnpm 명령어를 찾을 수 없음
```bash
npm install -g pnpm
```

---

## 📂 생성된 파일 구조

```
camping-site/
├── auto-install.bat          # Windows 자동 설치
├── auto-install.sh           # Mac/Linux 자동 설치
├── quick-start.py            # Python 자동 설치 (모든 OS)
├── setup-and-run.bat         # Windows 간단 실행
├── setup-and-run.sh          # Mac/Linux 간단 실행
├── START-HERE.txt            # 빠른 시작 가이드
├── README-실행방법.md         # 상세 실행 가이드
├── 실행하세요.md              # 이 파일
└── .env                      # 환경 변수 (자동 생성됨)
```

---

## 🚀 주요 API 엔드포인트

### 사용자 기능
- `POST /api/trpc/reservations.create` - 예약 생성
- `GET /api/trpc/reservations.myList` - 내 예약 조회
- `POST /api/trpc/paymentMethods.submitBankTransfer` - 계좌이체 증빙 제출
- `POST /api/trpc/inquiries.create` - 문의하기

### 관리자 기능
- `GET /api/trpc/reservations.adminList` - 모든 예약 조회
- `POST /api/trpc/reservations.updateStatus` - 예약 승인/거부
- `POST /api/trpc/paymentMethods.approveBankTransfer` - 계좌이체 승인
- `POST /api/trpc/sites.create` - 캠핑장 추가
- `POST /api/trpc/sites.update` - 캠핑장 수정
- `POST /api/trpc/bankAccounts.create` - 은행 계좌 추가
- `POST /api/trpc/paymentGateways.upsert` - 결제 게이트웨이 설정
- `POST /api/trpc/settings.update` - 사이트 설정 변경

---

## 💡 팁

### 개발 모드 vs 프로덕션 모드

**개발 모드 (자동 재시작):**
```bash
pnpm dev
```

**프로덕션 빌드:**
```bash
pnpm build
pnpm start
```

### 데이터베이스 초기화
```bash
# 마이그레이션 재실행
pnpm db:push
```

### 코드 포맷팅
```bash
pnpm format
```

### 타입 체크
```bash
pnpm check
```

### 테스트 실행
```bash
pnpm test
```

---

## 📞 지원

문제가 발생하면:
1. `README-실행방법.md`의 문제 해결 섹션 확인
2. 이슈 등록
3. 로그 확인 (콘솔 출력)

---

## 🎉 성공!

서버가 정상적으로 시작되면:
```
============================================================
  🚀 서버가 시작되었습니다!

     http://localhost:3000 에서 확인하세요

     종료하려면 Ctrl+C를 누르세요
============================================================
```

이 메시지가 보이면 성공입니다! 🎊
